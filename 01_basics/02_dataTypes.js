"use strict";   // treat all JS code as newer version

let name = "adi"
let age = 20
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => "" or ''
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique

// typeof use to get the datatype of value
//eg
console.log(typeof(age)) // both works
console.log(typeof age)

// object

console.log(typeof undefined); // undefined 
console.log(typeof null); // object -- null ka type is object